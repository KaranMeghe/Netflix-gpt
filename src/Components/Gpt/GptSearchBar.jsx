import { Input } from '../../Ui/index';

const GptSearchBar = () => {
    return (
        <div>
            <Input placeholder="Search with ai" />
        </div>
    );
};

export default GptSearchBar;